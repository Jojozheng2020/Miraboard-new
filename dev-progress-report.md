# MiraBoard 开发进度报告

**检查时间：** 2026-06-25 20:42  
**代码规模：** 前端 1,203 行 + 后端 286 行 + HTML 293 行  
**文件体积：** app.js 50 KB, styles.css 25 KB  
**最后修改：** 2026-06-25 19:18  
**Git 状态：** 刚初始化空仓库，无提交记录

---

## 模块完成度

### 已完成（10 个页面 + 全部交互）

| # | 模块 | 状态 | 说明 |
|---|------|------|------|
| 1 | **研究总览** | 完成 | 卡片墙 + 市场筛选（A股/港股/美股）+ 全局搜索 + 动态指标 |
| 2 | **雷达面板** | 完成 | 动态优先队列（过期/待刷新/反转尝试/失效位）+ 确认规则 |
| 3 | **标的库** | 完成 | 表格视图 + 市场筛选 + 搜索 + 点击进详情 |
| 4 | **行业视图** | 完成 | 按市场分组 + 对象数量 + 文件统计 + 点击进详情 |
| 5 | **资料库** | 完成 | 全局文件索引 + 动态类型筛选 + 只读文本预览 |
| 6 | **数据源状态** | 完成 | 8 项系统状态卡片（数据源/Mira路径/对象/文件/预览/行情/写入/队列） |
| 7 | **方法论库** | 完成 | 6 条研究边界规则展示 |
| 8 | **更新队列** | 完成 | 按 stale/needs_refresh 排序 + 直达详情 |
| 9 | **设置** | 完成 | 6 项配置展示（Mira路径/端口/写入/预览/行情/缓存） |
| 10 | **标的详情页** | 完成 | 多标签阅读器（memo/报告/证据/预期/资料库）+ 活动日志 |

### 核心交互功能

| 功能 | 状态 | 说明 |
|------|------|------|
| **全局搜索** | 完成 | 跨研究总览、标的库、资料库实时过滤 |
| **文件优先级排序** | 完成 | investment-memo → working-view → monitor → report-readout → evidence-log → 其他 |
| **Markdown 安全渲染** | 完成 | 标题/列表/段落渲染，220行截断，HTML 转义 |
| **文本预览缓存** | 完成 | 浏览器内存缓存，按文件路径索引 |
| **更新按钮（价格/新闻）** | 完成 | 只生成预览活动记录，不实际写入 |
| **批量刷新** | 完成 | 全部股价刷新预览，生成预览日志 |
| **文件摘要提取** | 完成 | 自动提取 `data_cutoff` / `must_refresh_if` / `core_conclusion` |
| **回退数据策略** | 完成 | API → bootstrap.json → 嵌入式兜底数据 |

### 后端 API（server.py）

| 端点 | 状态 | 说明 |
|------|------|------|
| `GET /api/health` | 完成 | 返回 Mira 根目录和索引状态 |
| `GET /api/bootstrap` | 完成 | 返回合并后的全量数据（静态 + 索引） |
| `GET /api/research-index` | 完成 | 扫描 `../Mira/private/research` 目录 |
| `GET /api/source-file` | 完成 | 只读预览文本文件（md/txt/csv/json/html），最大 60KB |
| 路径安全校验 | 完成 | 防止路径穿越，限制在 Mira 根目录内 |
| 文件类型推断 | 完成 | 按后缀识别，支持 Markdown 摘要提取 |
| 市场分类 | 完成 | A股(.SH/.SZ/.BJ)、港股(.HK)、美股(.US/.NASDAQ/.NYSE) |
| 刷新状态 | 完成 | 7天新鲜、30天过期 |

---

## 待接入 / 占位符

| 功能 | 当前状态 | 下一步 |
|------|----------|--------|
| **真实行情数据** | 显示"待接入" | 接入 Eastmoney / HK / US 路由 |
| **技术走势图** | 空状态占位 | 行情接入后渲染 SVG 价格线 + 点位 |
| **PDF 预览** | 不支持提示 | 接入页码定位/嵌入预览 |
| **Excel 预览** | 不支持提示 | 转为只读表格 |
| **写入 Mira** | 禁用，只生成预览 | 设计确认写入流程：生成预览 → 确认 → 写入 |
| **证据质量表** | 硬编码（恒瑞示例） | 从 Mira 文件动态解析 |
| **Thesis Ledger** | 硬编码 | 从 Mira 索引动态读取 |
| **反转/量能确认** | 静态规则 | 接入行情后计算 |
| **活动日志持久化** | 浏览器内存 | 写入 MiraBoard/data/activity-log |

---

## 关键边界说明

- **只读原则**：API 不写入 Mira，所有"更新"按钮只生成预览活动记录。
- **行情隔离**：未接入真实行情前，图表区域明确显示"行情路由待接入"，不展示模拟点位。
- **Mira 安全**：文件预览严格限制在 Mira 根目录内，60KB 截断防止大文件。
- **数据回退**：API 不可用时自动回退到本地 JSON，确保不白屏。

---

## 开发建议（优先级排序）

1. **接入 A 股行情（Eastmoney）** → 让价格、涨跌幅、技术走势图活起来
2. **接入港股/美股路由** → 覆盖全部 3 个市场
3. **设计写入确认流程** → 预览 → 确认 → 写入 Mira，生成 actual ingestion log
4. **PDF 内嵌预览** → 资料库的核心资料很多是 PDF
5. **动态证据质量** → 从 Mira 文件内容/时间/来源自动计算可信度
6. **Git 提交 + 远程仓库** → 当前是空仓库，建议先提交现有代码
